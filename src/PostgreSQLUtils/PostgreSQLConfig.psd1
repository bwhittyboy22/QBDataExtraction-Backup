@{
    PostgreSQL = @{
        Server = 'localhost'
        Port = '5432'
        Database = 'AmquipQBExtraction'
        Username = 'postgres'
        Password = 'REMOVES-handles-dreaded-remained'  # Consider using more secure password storage in production
    }
}